.idea/
vendor/
node_modules/
.DS_Store
.composer.lock
composer.lock
.phpunit.result.cache

# forget some files and folders inside the published CSS and JS assets, 
# since they're not actually used anywhere
src/public/packages/ckeditor/samples/
